import type {ArenaMap} from '@/lib/fps/maps';
export default function MapDiagram({map}:{map:ArenaMap}){return <svg className="map-diagram" viewBox="-26 -22 52 44" role="img" aria-label={map.name+' overhead layout'}><rect x="-24" y="-20" width="48" height="40" fill={map.ground} opacity=".22" stroke={map.accent} strokeWidth=".3"/>{map.walls.map((b,i)=><rect key={i} x={b.x-b.w/2} y={b.z-b.d/2} width={b.w} height={b.d} fill={b.h<1.5?map.accent:'#68777c'} opacity={b.h<1.5?.7:.65} stroke="#8ea4ad" strokeWidth=".15"/>)}{map.spawns.map((s,i)=><circle key={i} cx={s.x} cy={s.z} r=".5" fill={map.accent}/>)}<path d="M -2 0 H 2 M 0 -2 V 2" stroke={map.accent} strokeWidth=".35"/></svg>;}
