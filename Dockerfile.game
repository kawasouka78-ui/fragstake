FROM node:22-bookworm-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY lib ./lib
COPY server ./server
ENV NODE_ENV=production
ENV LIVE_HOST=0.0.0.0
ENV PORT=8080
EXPOSE 8080
CMD ["node", "--experimental-strip-types", "server/game-server.ts"]
